/** @type {import('next').NextConfig} */
const nextConfig = {
  sassOptions: {
    // SCSS configuration
  },
  // Optimized for Vercel deployment
  output: 'standalone',
};

module.exports = nextConfig;
